<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mani.lollipopexperiment" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/MainActivityTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".AllWidgetsActivity"
            android:theme="@style/AllMaterialColorTheme"/>

        <activity
            android:name=".ToolBarLightActivity"
            android:theme="@style/ToolBarLightTheme"/>

        <activity
            android:name=".ToolBarLightDarkActivity"
            android:theme="@style/ToolBarLightTheme"/>

        <activity
            android:name=".ToolBarDarkActivity"
            android:theme="@style/ToolBarDarkTheme" />

        <activity
            android:name=".RippleExampleActivity"
            android:theme="@style/RippleExampleTheme" />

        <activity
            android:name=".MyActivity1"
            android:label="@string/app_name"
            android:theme="@style/MyActivityTheme">

        </activity>

    </application>

</manifest>
